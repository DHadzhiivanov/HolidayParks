apiVersion: v1
kind: Service
metadata:
  name: booking-app
  namespace: fonteyn-dev
spec:
  selector:
    app: booking-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
